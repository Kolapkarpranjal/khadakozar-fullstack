# Production Environment Variables for Render.com
# Copy these and add them in Render Dashboard → Environment Variables

MONGODB_URI=mongodb+srv://khadakozar_admin:Khadak%40123456@cluster0.aj1km5i.mongodb.net/grampanchayat_khadak_ozar?retryWrites=true&w=majority&appName=Cluster0

PORT=5000

NODE_ENV=production

# IMPORTANT: Change this to a strong random string (at least 32 characters)
# Example: openssl rand -base64 32
JWT_SECRET=CHANGE_THIS_TO_STRONG_RANDOM_STRING_AT_LEAST_32_CHARACTERS_LONG

JWT_EXPIRE=7d

# IMPORTANT: Change these to your secure admin credentials
ADMIN_EMAIL=admin@khadakozar.com
ADMIN_PASSWORD=CHANGE_THIS_TO_STRONG_PASSWORD

# File Upload Configuration
MAX_FILE_SIZE=10485760
UPLOAD_PATH=./uploads

# CORS Origins (Update these with your actual frontend URLs after deployment)
# The server.js already handles this, but you can override if needed



